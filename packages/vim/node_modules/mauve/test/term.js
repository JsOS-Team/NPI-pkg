var mauve = require('../index');
mauve.set({
	test: '#0Fd'
});
describe('raw', function() {
	it('returns the raw string', function() {
		x = mauve('asdf');
		console.log(x.test);
		x.test.raw.should.equal(x.toString());
		console.log(x.test);
	});
});
